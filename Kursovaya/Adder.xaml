<Window x:Class="Kursovaya.Adder"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kursovaya"
        mc:Ignorable="d"
        Title="Adder" Height="300" Width="500"
        MinHeight="300" MinWidth="500">
        
    <Grid x:Name="Buttons">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Button Content="Преподователи" Grid.Row="0" Grid.Column="0" Style="{StaticResource AddButtons}"/>
        <Button Content="Дисциплины" Grid.Row="0" Grid.Column="1" Style="{StaticResource AddButtons}"/>
        <Button Content="Учебные планы" Grid.Row="0" Grid.Column="2" Style="{StaticResource AddButtons}"/>
        <Button Content="Группы" Grid.Row="1" Grid.Column="0"  Style="{StaticResource AddButtons}"/>
        <Button Content="Аудитории" Grid.Row="1" Grid.Column="1"  Style="{StaticResource AddButtons}"/>
        <Button Content="Курс" Grid.Row="1" Grid.Column="2"  Style="{StaticResource AddButtons}"/>
    </Grid>
</Window>
